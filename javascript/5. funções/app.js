function sum(number1, number2) {
    return number1 + number2
} 

// {} serve para declarar um escopo(ação de alguma coisa).

// 'number1' e 'number2' são chamados de parâmetros da função, mas quando a função e chamada e os valores são adicionados, esses valores se chamam 'argumentos'. Portanto o primeiro argumento irá preencher o primeiro parâmetro e assim sucessivamente.

// os parâmetros são visíveis apenas dentro da função.

console.log(sum(10, 50))

console.log(number1)