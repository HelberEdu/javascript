var name = 'Helber' // string
var age = 23 // integer
var weight = 64.5 // float
var alive = false // boolean
var food = null // null
var time // undefined

var composta = ['', 23, 66.8, false, null] // variavel composta

var fruits = ['maça', 'melão',] // variavel composta

var person = {
    name: name,
    age: 23,
    alive: true,
    height: 1.64,
} // object

console.log(typeof person); // typeof X checa o tipo da variável de nome X.

console.log(person);

console.log(person.name); // Objeto.Propriedade para acessar o valor da propriedade dentro do objeto.

console.log(fruits);

console.log(fruits[0]); // Array[Posição] para acessar valor em determinada posição.